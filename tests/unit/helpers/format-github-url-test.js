
import { formatGithubUrl } from 'shrink2/helpers/format-github-url';
import { module, test } from 'qunit';

module('Unit | Helper | format github url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatGithubUrl([42]);
  assert.ok(result);
});

